/** The auth surface the rest of the app imports. */

export { AuthProvider, useIdentity, displayNameFor } from './identity';
export type { Identity } from './identity';
export { AuthControl } from './AuthControl';
