# SPA fallback: Pages must serve index.html for unknown paths so deep links to
# /daily, /practice and /leaderboard work (§9.1, AC-036). /api/* is the Worker's.
/api/*  /api/:splat  200
/*      /index.html  200
