# Clerk's publishable key is public by design — it identifies the instance and
# is visible in the client bundle either way. The secret key is never here; the
# Worker verifies sessions against the JWKS and needs no secret at all.
VITE_CLERK_PUBLISHABLE_KEY=
